# Copy this file to .env.local and keep your real keys there.
# 1. Rename or copy: env.local.example  →  .env.local
# 2. Restart dev server: npm run dev

# MongoDB Atlas connection string (use %40 for @ in password)
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/

# Gemini (get key at https://ai.google.dev/)
# GEMINI_API_KEY=your_key_here
# GEMINI_MODEL=gemini-2.0-flash

# Google OAuth (for Sign in with Google)
# Get credentials at https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID (Web application)
# Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret

# NextAuth (required for Google sign-in)
# Generate: openssl rand -base64 32
# NEXTAUTH_SECRET=your_random_secret
# NEXTAUTH_URL=http://localhost:3000   (use your production URL when deploying, e.g. https://yourdomain.com)
