# SquidAI Screenshots

Add your SquidAI screenshots here so they appear in the main README on GitHub.

| File          | Description                          |
|---------------|--------------------------------------|
| `hero.png`    | Landing page / Hero section          |
| `chat.png`    | Chat interface with SquidAI          |
| `features.png`| Features or capabilities section      |

**Tips:**
- Use **PNG** or **JPG** (e.g. `hero.png`, `chat.jpg`).
- Recommended width: **1200–1400px** for a sharp look on GitHub.
- Take screenshots in light or dark mode (or both and name them `hero-dark.png` / `hero-light.png` if you prefer).

After adding files, update the image paths in the root `README.md` if you use different filenames.
